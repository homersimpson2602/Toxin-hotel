mixin item(title, descr)
  .item
    h2.item-title= title
    p.item-descr= descr

mixin navLink(link, name, first)
  li
    if(first)
      a(href=link).nav__link.nav__link_first=name
    else
      a(href=link).nav__link=name